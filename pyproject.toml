[project]
name = "kubernetes-learning"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi[sandard,sandart,standard]>=0.115.12",
    "pydantic-settings>=2.9.1",
]
